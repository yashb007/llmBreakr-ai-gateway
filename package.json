{
  "name": "llmbreakr-ai-gateway",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "build": "npm run build --prefix web",
    "start": "concurrently -n server,web -c blue,green \"npm start --prefix server\" \"npm start --prefix web\"",
    "install:all": "npm install --prefix server && npm install --prefix web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
