services:
  app:
    build: .
    image: yashbansal0412/llmbreakr-ai-gateway:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    ports:
      - "4000:4000" # gateway API + admin API
      - "3000:3000" # admin dashboard
